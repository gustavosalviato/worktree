trigger:
- build

pool:
  vmImage: ubuntu-latest

stages:
- stage:
  displayName: 'Stage Build & Test'
  jobs:
  - job:
    displayName: 'Build and Tests'
    steps:
    - task: UseDotNet@2
      displayName: 'Installing .NET SDK'
      inputs:
        version: 10.x

    - task: DotNetCoreCLI@2
      displayName: 'Build'
      inputs:
        command: build

    - task: DotNetCoreCLI@2
      displayName: 'Tests'
      inputs:
        command: test

